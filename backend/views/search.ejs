<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Pesquisa</title>
  </head>
  <body>
    <body>
      <h2>Search Results</h2>
      <% if (results.length > 0) { %>
      <ul>
        <% results.forEach(seller => { %>
        <li>
          <a href="/lojas/<%= seller.id %>"><%= seller.store_name %></a>
        </li>
        <% }); %>
      </ul>
      <% } else { %>
      <p>No results found.</p>
      <% } %>
    </body>
  </body>
</html>
